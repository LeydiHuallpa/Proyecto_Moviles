<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view" >

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_alarma"
            android:icon="@drawable/ic_add_alert_black_24dp"
            android:iconTint="@android:color/white"
            android:title="Configuracion de Alarma" />
        <item
            android:id="@+id/nav_buscar"
            android:icon="@drawable/ic_search_black_24dp"
            android:iconTint="@android:color/white"
            android:title="Buscar Helpers" />
        <item
            android:id="@+id/nav_horario"
            android:icon="@drawable/ic_date_range_black_24dp"
            android:iconTint="@android:color/white"
            android:title="Horarios de Helpers" />
        <item
            android:id="@+id/nav_listaProfesionales"
            android:icon="@drawable/ic_directions_car_black_24dp"
            android:iconTint="@android:color/white"
            android:title="Lista de Helpers" />
    </group>

    <item android:title="Comunicate con nosotros"
        android:iconTint="@android:color/white">
        <menu>
            <item
                android:id="@+id/nav_quejas"
                android:icon="@drawable/ic_person_black_24dp"
                android:iconTint="@android:color/white"
                android:title="Quejas y Sugerencias" />
            <item
                android:id="@+id/nav_cerrarsesion"
                android:icon="@drawable/ic_cancel_black_24dp"
                android:iconTint="@android:color/white"
                android:title="Cerrar Sesion" />
        </menu>
    </item>

    <item android:title="Comparte nuestra app"
        android:iconTint="@android:color/white">
        <menu>
            <item
                android:id="@+id/accion_compartir"
                android:icon="@android:drawable/ic_menu_share"
                android:iconTint="@android:color/white"
                android:title="Comparte la app" />

        </menu>
    </item>

</menu>
